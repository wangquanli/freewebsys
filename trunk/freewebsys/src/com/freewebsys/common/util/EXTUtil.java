package com.freewebsys.common.util;

import java.util.List;

import com.google.gwt.json.client.JSONArray;

public class EXTUtil {

	/**
	 * 返回分页的JSON的格式.
	 */
	public static String toPageJSON(int pageSize, List pageList) {
		/*
		 * JSONArray array = new JSONArray(); for (int i = 0; i <
		 * pageList.size(); i++) { array.add(pageList.get(i));
		 * System.out.println(pageList.get(i)); } String strJSON =
		 * "{\"results\":\"" + pageSize + "\",\"items\":" + array.toString() +
		 * "}";
		 */
		return null;
	}

}
